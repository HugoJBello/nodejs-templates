extends layout

block content
  if (entry)
    h1 #{entry.name}
    .container 
      div !=md(#{entry.content})
//    .container #{entry.category.name}
   if (!entry)
    h1 Editing #{entry.name}
    a(href="/entry_editor") edite #{entry.name}
